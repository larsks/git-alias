[metadata]
name = git_alias
version = attr: git_alias.__version__
description = Tools for managing git aliases
long_description = file: README.md

[options]
packages = find:

[options.entry_points]
console_scripts =
    git-alias = git_alias.main:main
